{
	"scripts": {
		"scaffold": "docker-compose build scaffold --progress=plain && docker-compose up scaffold && docker-compose cp scaffold:/usr/src/app ./app && docker-compose down scaffold",
		"package-lock": "docker-compose build package-lock --progress=plain && docker-compose up package-lock && docker-compose cp package-lock:/usr/src/app/package-lock.json ./app && docker-compose down package-lock"
	}
}
